---
name: thesis-reviewer
description: >-
  从五个维度审查毕业论文草稿：逻辑一致性、写作质量、
  禁用模式、图表质量和格式规范。当用户要求
  "审查论文"、"检查草稿"、"查找问题"、"校对"、
  "review the thesis"、"check the draft"、"find issues"、
  "proofread"，或在最终提交前使用。
---

# 论文审查器

## 概述

本技能审查毕业论文草稿，生成带有严重性标记的结构化报告。检查内容包括逻辑一致性、写作质量、禁用 AI 语气模式、图表质量，以及是否符合 INSTRUCTIONS.md 中的格式规范。

## 何时使用本技能

- 完成某一章节或整篇论文后。
- 最终提交前。
- 用户要求"审查论文"、"检查草稿"、"查找问题"、"校对"时。

## 何时不使用本技能

- 撰写新内容。请改用 `thesis-writer`。
- 创建图表。请改用 `mermaid-renderer`。

## 核心步骤

### 步骤 1：逻辑一致性检查

参见：references/consistency-checks.md 和 references/logic-and-structure.md

检查：
- 绪论流程图完整（背景、不足、目标、挑战、方法、贡献）。
- 绪论中的模块名称与方法论小节标题匹配。
- 每项贡献对应特定章节。
- 术语在所有章节中保持一致。
- 各章内图号顺序连续。

链条中的每一处断裂均为 CRITICAL 级别。

### 步骤 2：写作质量检查

参见：references/section-guides.md 和 references/logic-and-structure.md

检查：
- 每段都有主题句。
- 段落不超过 10 行；超出则拆分。
- 无重复或冗余段落。
- 学术语言风格（无口语化表达）。
- 代码到学术语言的转换已完成（正文中无原始代码片段）。

### 步骤 3：禁用模式扫描

参见：references/forbidden-patterns.md

扫描：
- 禁用的 AI 语气词汇：创新性、开创性、革命性、变革性框架、卓越、超越、杰出、前所未有、突破等。
- 破折号误用（破折号作为句子连接词）。
- 夸大表述（未加限定的"state-of-the-art"，将"comprehensive experiments"作为贡献）。

逐条标记严重性。论文中出现三处及以上禁用 AI 语气词即为 MAJOR 级别。

### 步骤 4：图表质量检查

参见：references/design-rules.md

对每张图表：
- 图题格式为 `**图 {章节号}-{序号} {标题}**`。
- 图表在正文中被引用后才出现。
- Mermaid 图表使用 `base` 主题（检查 .mmd 文件）。
- Markdown 中无 SVG 引用（仅 PNG）。
- 无图表垃圾（3D 效果、渐变、密集网格线）。

### 步骤 5：格式规范检查

对照 INSTRUCTIONS.md 检查：
- 各章字数在目标范围内。
- 正文中无超过 10 行的代码块。
- 需要的位置已插入实物照片占位符。
- 参考文献使用 `[@key]` 语法（非手动 `[1]`）。
- 图片路径使用 `../images/` 相对路径。

## 严重性分类

- **CRITICAL**：阻塞提交。示例：逻辑链断裂、夸大系统能力、伪造代码引用。
- **MAJOR**：审稿人会指出。示例：3 个以上段落缺少主题句、3 处以上禁用 AI 语气词、Markdown 中引用 SVG。
- **MINOR**：润色项。示例：两句过长的句子、冠词错误、轻微格式问题。

## 输出格式

### 摘要
- CRITICAL: n
- MAJOR: m
- MINOR: k
- 优先修复前三项：...

### 维度 1：逻辑一致性
| # | 发现 | 严重性 | 建议修复 |
|---|---|---|---|
| 1 | ... | ... | ... |

### 维度 2：写作质量
（同上表格格式）

### 维度 3：禁用模式
（同上表格格式）

### 维度 4：图表质量
（同上表格格式）

### 维度 5：格式规范
（同上表格格式）

### 最终评分（1-10）
（分数）

### 提交建议
- 可以提交 | 需再修改 1-2 天 | 需要大幅修改
